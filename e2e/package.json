{
  "name": "openerp-e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:headed": "HEADLESS=false node --test tests/*.test.mjs"
  },
  "dependencies": {
    "puppeteer-core": "^24"
  }
}
