load("@rules_rust//rust:defs.bzl", "rust_test")

rust_test(
    name = "macro_test",
    srcs = glob(["src/**/*.rs"]),
    edition = "2024",
    proc_macro_deps = [
        "//rust/lib/dsl/macro",
    ],
    deps = [
        "//rust/lib/dsl/client",
        "//rust/lib/dsl/types",
        "@crates//:flatbuffers",
        "@crates//:serde",
        "@crates//:serde_json",
    ],
)
