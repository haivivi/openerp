load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "client",
    crate_name = "openerp_client",
    srcs = glob(["src/**/*.rs"]),
    edition = "2021",
    visibility = ["//visibility:public"],
    deps = [
        "@crates//:reqwest",
        "@crates//:serde",
        "@crates//:serde_json",
    ],
)
