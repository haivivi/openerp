# API layer — schema definitions, codegen tools, and generated code.
#
# The schema/ crate contains all resource definitions (DSL).
# The codegen_lib/ crate provides code generation from IR.
# The macro/ crate provides proc-macro attributes (#[db_resource], etc.).
