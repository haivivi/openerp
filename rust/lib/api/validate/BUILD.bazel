load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

rust_library(
    name = "validate",
    crate_name = "openerp_dsl_validate",
    srcs = glob(["src/**/*.rs"]),
    edition = "2021",
    visibility = ["//visibility:public"],
    deps = [
        "//rust/lib/api/ir",
    ],
)

rust_test(
    name = "validate_test",
    crate = ":validate",
)
